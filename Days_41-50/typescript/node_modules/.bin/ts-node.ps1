#!/usr/bin/env pwsh
$basedir=Split-Path $MyInvocation.MyCommand.Definition -Parent

$exe=""
if ($PSVersionTable.PSVersion -lt "6.0" -or $IsWindows) {
  # Fix case when both the Windows and Linux builds of Node
  # are installed in the same directory
  $exe=".exe"
}
$ret=0
if (Test-Path "$basedir/node$exe") {
  # Support pipeline input
  if ($MyInvocation.ExpectingInput) {
<<<<<<<< HEAD:Days_41-50/typescript/node_modules/.bin/ts-node.ps1
    $input | & "$basedir/node$exe"  "$basedir/../ts-node/dist/bin.js" $args
  } else {
    & "$basedir/node$exe"  "$basedir/../ts-node/dist/bin.js" $args
========
    $input | & "$basedir/node$exe"  "$basedir/../typescript/bin/tsc" $args
  } else {
    & "$basedir/node$exe"  "$basedir/../typescript/bin/tsc" $args
>>>>>>>> b4b3b0f4440339f59e8bbbbebe75bb00d72a8a73:Days_41-50/typescript/node_modules/.bin/tsc.ps1
  }
  $ret=$LASTEXITCODE
} else {
  # Support pipeline input
  if ($MyInvocation.ExpectingInput) {
<<<<<<<< HEAD:Days_41-50/typescript/node_modules/.bin/ts-node.ps1
    $input | & "node$exe"  "$basedir/../ts-node/dist/bin.js" $args
  } else {
    & "node$exe"  "$basedir/../ts-node/dist/bin.js" $args
========
    $input | & "node$exe"  "$basedir/../typescript/bin/tsc" $args
  } else {
    & "node$exe"  "$basedir/../typescript/bin/tsc" $args
>>>>>>>> b4b3b0f4440339f59e8bbbbebe75bb00d72a8a73:Days_41-50/typescript/node_modules/.bin/tsc.ps1
  }
  $ret=$LASTEXITCODE
}
exit $ret
