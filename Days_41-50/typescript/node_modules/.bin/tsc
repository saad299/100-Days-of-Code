#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*|*MINGW*|*MSYS*)
        if command -v cygpath > /dev/null 2>&1; then
            basedir=`cygpath -w "$basedir"`
        fi
    ;;
esac

if [ -x "$basedir/node" ]; then
<<<<<<<< HEAD:Days_41-50/typescript/node_modules/.bin/tsc
  exec "$basedir/node"  "$basedir/../typescript/bin/tsc" "$@"
else 
  exec node  "$basedir/../typescript/bin/tsc" "$@"
========
  exec "$basedir/node"  "$basedir/../ts-node/dist/bin-esm.js" "$@"
else 
  exec node  "$basedir/../ts-node/dist/bin-esm.js" "$@"
>>>>>>>> b4b3b0f4440339f59e8bbbbebe75bb00d72a8a73:Days_41-50/typescript/node_modules/.bin/ts-node-esm
fi
